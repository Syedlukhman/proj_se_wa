version: "3.8"
services:
  web:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      SECRET_KEY: change-this-in-prod
    restart: unless-stopped
